title: TV Renamer
description: A Java GUI utility to rename TV episodes from TV listings

# The vendored copy of Cayman this replaces was a 2014 snapshot produced by
# GitHub's old automatic page generator. Pinned rather than floating, so the
# site cannot change appearance without a commit here.
remote_theme: pages-themes/cayman@v0.2.0
plugins:
  - jekyll-remote-theme
