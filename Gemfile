source "https://rubygems.org"

# Matches what GitHub Pages builds this site with. Pinning the whole
# github-pages set keeps the local preview and the deployed site on the
# same Jekyll, Liquid and theme versions.
gem "github-pages", group: :jekyll_plugins
